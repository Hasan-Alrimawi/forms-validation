import { CrossValidationDirective } from './cross-validation.directive';

describe('CrossValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new CrossValidationDirective();
    expect(directive).toBeTruthy();
  });
});
